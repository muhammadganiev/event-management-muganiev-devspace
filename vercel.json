{
  "buildCommand": "rm -rf .next node_modules && NODE_ENV=development npm install --legacy-peer-deps && NODE_ENV=production node_modules/next/dist/bin/next build",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "devCommand": "node_modules/next/dist/bin/next dev",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "crons": []
}
